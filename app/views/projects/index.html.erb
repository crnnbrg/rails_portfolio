<h1>Projects</h1>

  <ul>
    <% @projects.each do |project| %>
      <li><%= link_to project.title, project_path(project) %></l
    <% end %>
  </ul>

  <hr>
   <%= link_to "New Project", new_project_path %>
