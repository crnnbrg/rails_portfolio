<h1>Users</h1>

  <ul>
    <% @users.each do |user| %>
      <li><%= link_to user.name, user_path(user) %></li>
      <li><%= user.email %></li>
      <li><%= user.username %></li>
    <% end %>
  </ul>
